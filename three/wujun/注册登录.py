#!/usr/bin/env python#coding:utf-8import random#登录用户，用户错误提示注册，密码小于6位，输错三次，退出def signin():    with open('/Users/wj/Desktop/actual-15-homework/three/wujun/user.txt', 'r') as f:        Users = {}        for i in f.readlines():            Temp = i.strip("\n").split(":")            Users[Temp[0]] = Temp[1]    UserName = raw_input("请输入你的名字：").strip()    if UserName in Users.keys():        # print Users[UserName]        n = 3        while n > 0:            Passwd = raw_input("请输入你的密码：")            # 判断密码长度是否大于等于6位            if len(Passwd) >= 6:                if Passwd == Users[UserName]:                    res = "欢迎回来，%s" % UserName                    break                else:                    n = n - 1                    if n == 0:                        print "三次机会用完了，再见"                        break                    res = '密码错误，请重试,还剩余 %s次' % n            else:                res =  "密码需要六位以上，请重试"                continue    else:        res = "用户不存在,请注册，谢谢"    return res#用户注册，随机校验码，密码不能小于6位def signon():    with open('/Users/wj/Desktop/actual-15-homework/three/wujun/user.txt', 'r') as f:        Users = {}        for i in f.readlines():            Temp = i.strip("\n").split(":")            Users[Temp[0]] = Temp[1]    UserName = raw_input("请输入你的名字：")    if UserName in Users.keys():        res = "用户已存在，请登录"    else:        n = 0        while n < 3:            Passwd = raw_input("请输入你的密码，注意密码需要超6位：")            if len(Passwd) < 6:                res = "密码小于6位，请重试"                n += 1            else:                Templist = random.sample('qwertyuiop', 4)                Str = "".join(Templist)                print Str                Suiji = raw_input("请输入随机串：").lower()                if Suiji == Str:                    #文件追加写入是a，覆盖写入是w                    with open('/Users/wj/Desktop/actual-15-homework/three/wujun/user.txt', 'a') as f:                        f.write("%s:%s" % (UserName, Passwd))                    res = "恭喜你注册成功"                    #注册后要退出，要不循环依旧                    break                else:                    res = "验证码错误，请重试"                    n += 1            if n == 3:                res = "错误次数太多，请重新注册"                break    return res#用户不注册也不登录，提醒def null():    res ="请输入注册或者登录，谢谢"    print  res    start()#登录入口，1就是登录，0就是注册def start():    Shuru = raw_input("请选择登录还是注册，登录请按1，注册请按0：").strip()    if Shuru == '1':        res = signin()    elif Shuru == '0':        res = signon()    else:        res = null()    return resStartup = start()print Startup